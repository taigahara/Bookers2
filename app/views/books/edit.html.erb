<% if flash[:notice] %>
<p id="notice">
 <%= flash[:notice] %>
</p>
<% end %>

<div class="col-xs-9" >
<table class="table table-striped">
<h1>Editing Book</h1>
<% if @book.errors.any? %>
    <div id="error_explanation">
  <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>
<%= form_for(@book) do |f| %>
  <div class='field'>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class='field'>
    <%= f.label :opinion %><br>
    <%= f.text_field :body %>
  </div>
  <div class='actions'>
    <%= f.submit %>
  </div>
<% end %>

<td><%= link_to 'Show', book_path(@book), class: "btn btn-default" %></td>
<td><%= link_to 'Back', books_path, class: "btn btn-default" %></td>
</table>
</div>