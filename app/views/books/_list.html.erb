<% if flash[:notice] %>
<p id="notice">
 <%= flash[:notice] %>
</p>
<% end %>

 <h1>User info</h1>

    <div class="col-xs-3">
      <div>
        <p><td><%= attachment_image_tag user, :profile_image, :fill, 60, 60, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: "no_image.jpg" %></td></p>
        <p><span class="space">name</span><%= user.name %></p>
        <p><span class="space">introduction</span><%= user.introduction %></p>
        <% if user == current_user %>
          <p><%= link_to "編集", edit_user_path(user), class: "btn btn-default" %></p>
        <% end %>
      </div>

      <h2>New book</h2>
      <% if book.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

            <ul>
              <% book.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

      <%= form_for(book) do |f| %>
      <div class="field">
        <p><%= f.label :title %></p>
        <p><%= f.text_field :title %></p>
      </div>
      <div class="field">
        <p><%= f.label :Opinion %></p>
        <p><%= f.text_field :body %></p>
      </div>
      <div class="actions">
        <%= f.submit %>
      </div>
      <% end %>
  </div>
<div>